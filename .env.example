# Database connection URL
# For libpq-compatible SSL behavior with Prisma/pg, keep both query params.
# Use sslmode=verify-full instead when you want certificate hostname verification.
DATABASE_URL="postgresql://username:password@host/database?sslmode=require&uselibpqcompat=true"
